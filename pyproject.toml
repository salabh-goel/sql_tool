[project]
name = "postgres_tool"
version = "1.0.1"
description = "Read-only PostgreSQL tool suite for CrewAI agents"
readme = "README.md"
requires-python = ">=3.10,<3.14"
dependencies = [
    "crewai[tools]>=1.13.0",
    "sqlalchemy>=2.0",
    "psycopg2-binary>=2.9",
]

[tool.crewai]
type = "tool"
